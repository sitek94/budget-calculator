import App from 'app';
import * as React from 'react';
import ReactDOM from 'react-dom';
import 'styles.scss';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
